<p style="color: green"><%= notice %></p>

<%# <h1>My Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%# <p>
      <%= link_to "#{list.name}", list_path(list)%>
    <%# </p>
  <% end %>
<%# </div> %>

<%# <%= link_to "New list", new_list_path %>

<div class="cover mb-4 d-flex align-items-center justify-content-center">
  <div class="text-center text-black">
    <h1>📽️ Anna's Movie Theatre 🎞️ </h1>
    <h3 class="typed-js" data-controller="typed-js"></h3>
  </div>
  </div>

  <br>
  
  <div class="container py-5">
    <div class="d-flex align-items-center justify-content-between mb-3">
      <h3 class="mb-0">My Lists</h3>
      <%= link_to "Create a movie list", new_list_path, class: "btn btn-flat" %>
    </div>

    <div class="row">
      <% @lists.each do |list| %>
      <div class="col-12 col-sm-6 col-md-4 mb-3">
        <%= link_to list_path(list), class: "text-decoration-none text-dark" do %>
          <div class="card card-category" style="background-color: <%= @background_colors.sample %>;">
            <div class="card-body">
              <h5 class="card-title mb-0"><%= list.name.capitalize %></h5>
          </div>
        </div>
      <% end %>
      </div>
          <% end %>
    </div>
  </div>
</div>
