<p style="color: green"><%= notice %></p>

<h1>My Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <p>
      <%= link_to "#{list.name}", list_path(list)%>
    </p>
  <% end %>
</div>

<%= link_to "New list", new_list_path %>

